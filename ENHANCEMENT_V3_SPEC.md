# 校务智汇中台 3.x 增强规格（ENHANCEMENT_V3_SPEC）

> 目标：修 bug + 交互友好化 + 图谱/采集/知识对象功能补全。按优先级 P0(修错)→P1(功能)→P2(体验)。
> 所有项：`docker compose up -d --build backend frontend` 后把对应页面冒烟 200 无 500 即视为通过。

---

## P0 · Bug 修复 / 交互修正

### P0-1 图谱可视化力导向：鼠标聚焦 & 滚轮冒泡
- **现状**：需 `roam`(缩放/拖拽)，但鼠标"聚焦在网页"时滚轮变成页面上下滚动、图上抓不动。
- **根因**：ECharts 容器未拦截滚轮 / 未独占焦点，滚轮事件冒泡到外层导致页面滚动。
- **改法**：`GraphForce.tsx` 给图表容器绑定 `wheel`(passive:false) → `preventDefault` + `stopPropagation`；`useEffect` 里 `el.addEventListener('wheel', handler, {passive:false})`，清理时移除；确保容器 `touch-action: none`。
- **验收**：鼠标在图上滚轮只缩放、不滚页面；可拖拽；空白区拖拽平移。

### P0-2 图谱页「构建/更新图谱」按钮卡住
- **现状**：点击后 `building` 态但长耗时(批量 LLM)无进度，像"卡住"。
- **改法**：按钮 `loading`(spinner) 已有，但改为更强反馈；页面右侧固定一个**流程面板**，构建期间用 Ant `Steps` 展示"抽取 → 建关系 → 入库 → 完成"阶段动画，完成后显示结果(新增关系/实体数/耗时/跳过)；失败显示错误。
- **验收**：构建中按钮转圈 + 右侧流程框推进；完成/失败有明确结果。

### P0-3 已归档(ARCHIVED)知识语义说明
- **现状**：检索 `search_knowledge` 默认 `status=="PUBLISHED"`，故 ARCHIVED/EXPIRED/REVIEW_REQUIRED 均不被检索/引用。
- **结论**：已归档知识确实**不参与检索、不被引用**（设计如此=下线）。
- **改法**：知识对象列表 ARCHIVED 标签加 tooltip「已归档，不参与 AI 引用」；spec 明确此语义。

---

## P1 · 功能补全

### P1-1 知识图谱页新增实用功能（不只摆着看）
- 节点/关系**点击 → 右侧详情面板**：实体名/类型/关联知识对象、一跳邻居列表、相关关系。
- **类型筛选**：按 部门/政策/事件/对象/时间/文件 高亮过滤。
- **实体/关系统计**：卡片显示各类型数量。
- **图谱问答说明**：明确"关系型问题（谁发布/适用于谁/截至何时）"的用途，作为入口说明文案。

### P1-2 数据源「自动发现」预置广州大学相关站点
- 打开自动发现弹窗时，默认展示一组**预置推荐源**：广州大学官网、教务处、研究生院、学生处、图书馆、招生办、新闻网等（可直接勾选添加），用户无需先输入 URL。
- 保留"输入 URL 智能推荐"能力。

### P1-3 删除数据源行的「上传文件」操作
- 移除 `Sources.tsx` 行内 `Upload/上传文件` 按钮（改为在知识对象页做文件入库）。

### P1-4 采集弹窗功能完善
- 保留页数/内容筛选(现有 4 栏目)，增加：**采集深度提示**、可选「截止时间范围」、结果摘要预览。
- 内容筛选下拉确保有更多栏目选项并生效。

---

## P2 · 体验 + 文件入库

### P2-1 采集任务界面友好化
- 弃用"一堆数据字段直曝"，改用**卡片/时间线**：状态徽章、阶段步骤条、结果摘要(抓取/新增/跳过)、耗时。

### P2-2 知识对象页「上传文件」入库
- 在「新增知识对象」按钮右侧加「上传文件」；
- 上传 **md/pdf/txt** → 解析文本 → **LLM 抽取**(标题/类型/部门/摘要/关键信息) → 创建知识对象；
- 后端新增 `POST /knowledge-objects/ingest-file`（复用 DocumentParser + 新增 LLM 抽取 agent，失败降级规则式）。

---

## 交互说明（答复用户疑问）
- **图谱问答作用**：回答"关系型/多跳"校务问题，如“研究生奖助学金办法由谁发布、适用于谁、何时截止”，依赖知识图谱的实体-关系结构做推理，与融合检索互为补充。
- **已归档知识**：默认**不参与检索、不被引用**（检索只取 PUBLISHED），归档即下线。
