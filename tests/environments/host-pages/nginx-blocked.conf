server {
    listen 80;
    server_name localhost;
    root /usr/share/nginx/html;
    index index.html;

    location / {
        try_files $uri $uri/ =404;
    }

    # Proxy SDK requests to backend
    location /sdk.js {
        proxy_pass http://backend-dev:8000/sdk.js;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
}
