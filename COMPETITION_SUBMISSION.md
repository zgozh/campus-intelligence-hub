# 参赛申报要点 —— 校务智汇中台 2.0

> 赛道：第二届「庆园杯」AI 创新应用大赛 · 类别③ 面向校务管理的 AI 自动数据采集与知识管理中台
> 一句话定位：**让高校校务信息"自动更新、可信可查、AI 可用"的一站式知识中台**，而非一个聊天机器人。

---

## 1. 真实痛点（贴合教育领域真实场景）

高校校务信息长期存在五个现实问题，本方案逐一回应：

| 现实痛点 | 具体表现 | 本方案回应 |
| --- | --- | --- |
| **散** | 通知/制度/办事流程分散在教务处、研究生院、学生处、财务处、图书馆等十几处官网，师生找不到、找不全 | 统一数据源管理 + AI 智能推荐采集 |
| **变** | 政策频繁修订、截止日期常变，人工盯着官网更新成本极高 | LLM 语义变化检测 + 变更雷达 + Diff 视图 |
| **旧** | 旧通知挂在新通知下方，师生引用过期信息 | 新鲜度/时效融合评分 + 临期提醒 + 一键归档过期 |
| **乱（可信）** | 网传信息与官方不一致，AI 引用无出处 | 证据优先引用 + Answer Guard 拒答 + 冲突消解 |
| **难复用** | 每个 AI 应用各自爬校务数据，重复建设 | 中台 API / MCP 标准接口，一次建设多方复用 |

## 2. 创新点（可落地的差异化）

1. **全自动采集闭环**：来源发现 → LLM 智能推荐采集 → 自动采集 → 三元组/事实抽取 → 自动更新，全程无需人工巡检。参考 Crawl4AI/Scrapling 的"采集+内容抽取"范式，但没有重复造轮子，直接复用成熟 OSS（Scrapling、Qdrant、pgvector）。
2. **LLM 校务知识图谱 + GraphRAG 融合问答**：从知识对象自动抽取"部门-政策-事件"实体关系，图谱多跳推理 + 融合检索双通道，回答"研究生奖助学金办法由谁发布、适用于谁"这类关系型问题。
3. **RAG 增强**：融合评分（语义+关键词+权威度+新鲜度+时效）+ **交叉编码精排（gte-rerank-v2）** + **意图/部门路由**，显著提升"证据引用准确率"。
4. **AI 审核助手 + 冲突消解**：LLM 对低置信/冲突知识给出摘要、风险与推荐动作（批准/拒绝/合并），管理员 AI 把关，治理从"人工翻看"升级为"AI 预审 + 人审"。
5. **AI 校务洞察**：基于运营数据（新增/变更/冲突/审核积压/来源异常/临期）自动生成"本期要点/趋势/风险/建议"。
6. **可信优先**：Evidence-first 引用标注（来源编号、时效、权威度、有效期）+ Answer Guard 无依据拒答模板，**从机制上防幻觉**。
7. **知识健康度公开公式**：`100 - 待审×2 - 冲突×3 - 陈旧占比×40 - 来源异常占比×30 - 过期占比×20`，运营效果量化可视。

## 3. 可量化指标（答辩/演示口径）

| 维度 | 指标 | 口径 |
| --- | --- | --- |
| 问答可信 | **Grounded Rate** | 有证据引用的问题占比（Answer Guard 生效） |
| 问答可信 | **引用准确率** | 人工评：答案关键点是否都能在引用来源中找到 |
| 采集 | **采集成功率** | 成功采集/计划采集（含滚动增量） |
| 采集 | **变化检测召回** | 人工抽验页面实际变化的发现率 |
| 知识治理 | **知识健康度** | 上述公开公式 |
| 治理效率 | **AI 预审采纳率** | 人工最终动作与 AI 建议一致的比例 |
| 复用 | **开放接口数** | REST + MCP 工具数（campus_search/source/knowledge/changes/review） |

> 部署即得：一键 `docker compose up -d --build`，无本地模型/GPU（全部走 DashScope 云 API），可空 API key 演示（Mock 降级链路，Zero-API Demo）。

## 4. 演示脚本（建议 3–5 分钟）

1. **开场**（30s）：一句话定位 + 真实痛点（校务信息散/变/旧）。
2. **数据自动采集**（60s）：数据源管理 → "AI 智能推荐数据源" → 触发一次采集任务，展示抓取进度。
3. **变化检测**（45s）：变更雷达（按部门/级别过滤）→ 打开某条 Diff 视图，展示"标题/正文/日期"高亮对比。
4. **知识治理**（45s）：知识对象列表 → 一键归档过期 → 审核队列 → 点「AI 建议」，展示 AI 预审（摘要/风险/推荐动作）。
5. **Ask AI（可信问答）**（45s）：提问校务问题，展示证据引用（来源编号+时效+权威度）与 Answer Guard 拒答样例。
6. **知识图谱**（45s）：构建/更新图谱 → 力导向可视化 → GraphRAG 关系问答。
7. **AI 校务洞察**（30s）：生成今日洞察，展示本期要点/趋势/风险/建议。
8. **收尾**（15s）：中台 API/MCP 复用 + 一键部署 + 可量化指标。

## 5. 技术栈（复用成熟 OSS，非从零自研）

FastAPI + Next.js14(App Router) + Qdrant(向量) + PostgreSQL(pgvector) + Redis + Scrapling(采集微服务)；LLM/Embedding/Rerank 走 DashScope（qwen-plus / text-embedding-v3 / gte-rerank-v2，OpenAI 兼容）。可选依赖全部降级：无 key 时走 Mock，主链路永远可用。
