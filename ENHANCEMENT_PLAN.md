# Campus Intelligence Hub 2.0 — AI 增强方案 + 待改进清单

> 背景：参加「面向校务管理的 AI 自动数据采集与知识管理中台」赛道。目前大模型主要用在问答/摘要，参与度偏低，需找到让 LLM 更深参与、更聚焦"知识中台"主题的增强点。
> 本文件 = ①现有问题修复清单（明确待办）②大模型增强方向（研究结论 + 开源参考）③推荐实施顺序。**大模型增强部分先出方案，确认后再开工。**

---

## 一、现有问题修复清单（明确待办，直接改）

| # | 问题 | 处理 |
|---|------|------|
| 1 | **注册功能不能正常使用** | 排查 register 端点/前端注册流程，修复 |
| 2 | **知识对象人工新增** | KO 页加「新增」→ 弹出填表卡（类型/标题/部门/有效期/标签/事实/摘要）→ 确认新建 KO（除爬取外人工录入） |
| 3 | **KO 列表字段排序** | 类型/状态/置信度/有效期等列支持排序（antd Table sorter） |
| 4 | **批量操作/一键归档** | KO 列表多选（rowSelection）→ 批量归档；「一键归档」把已过期(EXPIRED)全部→ARCHIVED |
| 5 | **去掉左侧栏「用户管理」** | 框架残留（Basjoo 管理员管理），移除菜单项（保留后端接口或一并收敛） |

---

## 二、大模型增强方向（让 LLM 更多参与 + 贴合"自动采集与知识中台"）

> 核心思想：把项目从"规则 + 检索"升级为 **LLM Agent 驱动的知识中台**——LLM 不只回答问题，而是**理解校务知识、管理知识生命周期、驱动采集与治理**。

### 方向 A：智能抽取器 → 结构化知识 + 知识图谱（优先级最高）
- **现状**：知识对象化靠规则（标题关键词分类/规则抽取字段），LLM 只在摘要。
- **增强**：LLM 从清洗后正文**语义抽取**完整知识对象（类型/部门/有效期/事实字段/对象/标签），并抽**实体-关系**（如「研究生院→发布→奖助学金办法，适用→全日制研究生，截止→9/25」）构建**校务知识图谱**。
- **开源参考**：[DocPolicyKG（LLM 从中文政策文档构建知识图谱）](https://dl.acm.org/doi/pdf/10.1145/3746252.3760904)、[元景万悟 UniAI-GraphRAG](https://www.zqbao.com.cn/news/10786.html)。
- **价值**：让 LLM 负责"理解校务文档"，是本赛道最贴合的差异化。

### 方向 B：语义变化检测 + 变更摘要
- **现状**：变化检测靠 content_hash 比对（只判"变没变"）。
- **增强**：LLM 比较新旧正文，判断**语义级变化**（关键日期/政策主体/适用范围/取消还是修改）+ 生成一句话**变更摘要**，决定 severity。Diff Viewer 更智能。
- **开源参考**：结合现有 Change Radar；LLM 摘要参考 RAGFlow。

### 方向 C：智能采集编排（Collection Agent）
- **增强**：LLM 从"自动发现"的候选栏目里**智能筛选高价值源**（过滤导航/垃圾页、识别通知公告/制度类栏目）+ 判断采集频率/优先级；定时触发采集。
- **开源参考**：[Crawl4AI / Firecrawl 类比（AI 驱动的爬虫）](https://github.com/remijul/web-scraping)、[2025 开源 AI Agent 全景](https://zhuanlan.zhihu.com/p/1992410866923091778)。
- **价值**：让 LLM 参与"采集"决策，贴合"自动数据采集"。

### 方向 D：RAG 增强（Answer Agent 智能化）
- **增强**：
  - **Rerank 精排**：接 `gte-rerank-v2`（DashScope 已预留）重排检索结果；
  - **查询改写/HyDE**：LLM 把问题改写成检索友好 query / 生成假设文档检索；
  - **意图理解 + 部门路由**：识别"办事/政策/查询/投诉"意图并路由到对应部门知识；
  - **GraphRAG 融合**：图谱关联推理增强召回。可结合现有融合评分。
- **开源参考**：[RAGFlow（深度文档理解 RAG）](https://github.com/pellera9/ragflow)、[Campus AI RAG（高校问答）](https://github.com/liweisi91010/Campus-AI-RAG)、[KnowFlow](https://github.com/knowflow-ai/KnowFlow)。

### 方向 E：AI 审核助手（Review Agent）
- **增强**：LLM 自动生成审核摘要 + 推荐动作（Approve/Reject/Edit）+ 冲突消解建议；低置信知识自动标记原因。让 LLM 参与"知识治理"。

### 方向 F：校务知识洞察/日报（LLM 洞察）
- **增强**：LLM 从当日新增/变更/冲突生成**校务知识洞察日报**（Top 变化、趋势、风险预警），不只做模板汇总。

### 方向 G：Agent 三层闭环（spec §28 已定义）
- Collection Agent → Knowledge Agent → Answer Agent，三者串成完整智能闭环，LLM 在各环节决策。

---

## 三、推荐实施顺序（先方案后开工）

**让大模型参与度最高、最聚焦主题的 Top3**：
1. **方向 A 智能抽取器 + 知识图谱**（LLM 理解校务知识，差异化最大）
2. **方向 B 语义变化检测 + 变更摘要**（把"变化检测"从技术比对升级为语义理解，答辩亮点）
3. **方向 C 智能采集编排**（LLM 驱动采集决策，贴合"自动采集"）

其次：**D RAG 增强**（Rerank/意图/GraphRAG，提升问答质量）、**E AI 审核助手**、**F 洞察日报**、**G Agent 闭环**。

> 注：现有基础设施（融合评分/Answer Guard/MCP/对外API）全部保留，本次增强是"加 LLM 语义层"，不重写。

---

## 四、关键权衡（DECISION）
- **不引入重型框架**：参考上述 OSS 的**模式**（LLM 抽取/图谱/编排），但不整仓引入 RAGFlow/GraphRAG 全套（避免 docker 变重 + License/复杂度）。坚持"能缝就缝"。
- **模型统一**：全部走 DashScope（qwen-plus 抽取/改写/摘要 + text-embedding-v3 + gte-rerank-v2 重排），无 key 自动 Mock，保证 Zero-API Demo 可用。

> **请确认**：大模型增强方向 A-G 是否认可？优先做哪几个（我建议 A+B+C 先行）？确认后我按此写 SPec/TASKS 再开工；同时前 5 点明确问题我并行直接修。
